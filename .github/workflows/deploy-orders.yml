name: Deploy Orders Apps Script

on:
  push:
    branches: [main]
    paths:
      - 'Orders'
      - '.github/workflows/deploy-orders.yml'

jobs:
  deploy-orders:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Prepare Apps Script project
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p apps-script
          cp Orders apps-script/Orders.gs
          cat > apps-script/.clasp.json <<'EOF'
          {
            "scriptId": "14RxieTk5ngsUaxwdb2IC3OzkpiFcCd7-1Xh0ydwoH0F3-ENL5ZuBU48H"
          }
          EOF
          cat > apps-script/appsscript.json <<'EOF'
          {
            "timeZone": "Asia/Yekaterinburg",
            "dependencies": {},
            "exceptionLogging": "STACKDRIVER",
            "runtimeVersion": "V8",
            "webapp": {
              "executeAs": "USER_DEPLOYING",
              "access": "ANYONE_ANONYMOUS"
            }
          }
          EOF

      - name: Setup clasp credentials
        shell: bash
        run: |
          set -euo pipefail
          printf '%s' '${{ secrets.CLASPRC_JSON }}' > ~/.clasprc.json

      - name: Push source to Apps Script
        working-directory: apps-script
        run: npx @google/clasp@latest push --force

      - name: Update existing deployment
        working-directory: apps-script
        env:
          COMMIT_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          npx @google/clasp@latest create-deployment \
            --deploymentId "AKfycbz2XQn7s0e_irZ2rRsvcXb_I7hKp_DxNXTYsZlIt2TATE58IiqJ9AyjUKj9f09-CII9" \
            --description "GitHub ${COMMIT_SHA:0:7}"
